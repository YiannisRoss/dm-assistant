

 <% mapImageURLs = [] %>
 <% i = 0 %>

  <% @maps.each do |map| %>

      <%mapImageURLs[i] = url_for(map.image) if map.image.persisted?%>
      <%i = i+1%>
 <%end%>  

<%= react_component("GameTracker", { current_user: current_user, characters: @characters_with_img_URL, maps: @maps, mapImageURLs: mapImageURLs }) %>
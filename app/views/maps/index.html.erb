<h1>Your maps </h1>

<%= link_to "Create new map", new_map_path%>
<br>
<% @maps.each do |map| %>
    <%= link_to map.title, map_path(map) %> 
    <a href=<%=map_path(map)%> > <img src="<%=(url_for(map.image))%>">   
<% end%>